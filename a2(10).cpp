//to check whether a number is divisible by 7 or not.
#include<stdio.h>
main()
{
	int n;
	printf("Enter Number:");
	scanf("%d",&n);
	if(n==999)
    {
	 printf("%d largest three digit number",n); 
    }
    else
    {
     printf("Invalid");
	}
	return 0;
}
